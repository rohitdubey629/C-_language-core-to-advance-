#include <stdio.h>
#include <conio.h>

int main()
{

    int a, b, c, d, sum;
    a = 10;
    b = 20;
    c = 30;
    d = 40;
    sum = (a + b) + (c + d);

    printf("total number of sum  %d + %d + %d + %d  = %d ", a, b, c, d, sum);

    return 0;
}